# Auto detect text files and perform LF normalization
* text=auto

# Windows batch scripts require CRLF (cmd.exe mis-parses LF-only .bat/.cmd)
*.bat text eol=crlf
*.cmd text eol=crlf

# Shell scripts must stay LF (CRLF breaks the shebang / bash)
*.sh text eol=lf
